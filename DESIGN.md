# BARREL DESIGN.md（2026-09 再設計）

## レーンと主題
- レーン: **編集メディア（brand寄り）**。ただし記事一覧・ハブ一覧は「読ませる密度」を優先する（新聞のセクションフロント）。
- Subject: 部活の監督・保護者・選手が、練習後の夜にスマホで、目の前の課題について**科学的根拠に基づく答えを1つ持ち帰る**。
- 非目標: 速報ニュース、ランキング一辺倒のまとめサイト、アフィリエイト色の強い比較サイト。

## ベンチマークから採る型
| 参照 | 採るもの |
|---|---|
| The Guardian / NYT セクションフロント | 1本のリード＋2本のサブ＋テキスト密度の高い最新リスト。「よく読まれている」レール。日付は必ず出す |
| The Athletic | 大きなセリフ見出し、大判リード画像、トピック（ハブ）単位の回遊 |
| Axios Smart Brevity | 記事冒頭の「結論 / 数字で見る / 今日からできること」ボックス（本文の結論段落を要約したもの） |
| Examine.com / Stronger by Science | 出典数・更新日・固定目次。根拠の見える化がE-E-A-T |
| Morning Brew | 本文中と記事末の購読（LINE）導線を1つのフォーマットで統一 |
| Google Discover 要件 | 1200px幅以上のリード画像、`max-image-preview:large`、著者表記、更新日 |

## パレット（CSS変数。生値をコンポーネントに書かない）
| 役割 | 変数 | HEX | 用途 |
|---|---|---|---|
| 支配色 | `--c-paper` | #F8F7F3 | ページ背景（面積70%以上） |
| 面1 | `--c-paper-2` | #EFEDE6 | 帯・レール背景 |
| 前景 | `--c-ink` | #14201A | 本文（緑に寄せた黒。純黒不使用） |
| 前景弱 | `--c-ink-muted` | #5F6B64 | 日付・補助（緑寄りグレー） |
| 境界 | `--c-line` | #D9D7CE | 罫線 |
| ブランド | `--c-green` | #0D3320 | ロゴ・ヘッダー・フッター・見出し罫 |
| ブランド濃 | `--c-green-deep` | #08211A | フッター下段 |
| アクセント | `--c-beige` | #E8D5B0 | ロゴの走者色。**CTAボタンとシグネチャのみ**（5%未満） |

## 書体（3ロール）
- Display / Body: **OS標準の角ゴシック**（Hiragino Sans / Noto Sans JP / Meiryo）。見出し700・本文400で、見出しと本文の差はサイズと太さでつける。和文Webフォントは使わない（数百スライスで表示が遅れ、Yahoo!ニュース・noteもOS標準書体）。
- スマホ本文: デジタル庁デザインシステム Std-16N-175（16px・行間175%・字間2%）。列の多い表は「項目名｜値」のブロックに組み替え、横スクロールさせない。
- Utility: **Oswald** 500（英字の eyebrow・カテゴリラベル・数字の強調、大文字＋字間0.12em）、**IBM Plex Mono** 400（日付・出典数・読了時間）。

## レイアウト方針
新聞のセクションフロント。12カラム、幅の変化（全幅の緑帯 ↔ 1240px記事幅）で単調さを破る。記事ページは本文も表も画像も1240px記事幅に揃える。
```
HOME
┌───────────────────────────────┬──────────────┐
│ LEAD（リード画像＋大見出し＋結論1行）  │ SUB ×2（画像小）│
├───────────────────────┬───────┴──────────────┤
│ 最新（テキスト密度の高いリスト） │ よく読まれている（番号付き）│
├───────────────────────┴──────────────────────┤
│ ■ テーマ別（緑帯）: 5ハブ × [pillar + 最新3]                  │
├──────────────────────────────────────────────┤
│ LINEで受け取る（ベージュのボタン1つ）                        │
├──────────────────────────────────────────────┤
│ BARRELの編集方針（出典・PR表記・修正方針 = E-E-A-T）           │
└──────────────────────────────────────────────┘
ARTICLE
パンくず → eyebrow → 見出し → 要約 → メタ（公開/更新/読了/出典数/編集部） → リード画像
→ [結論ボックス（本文先頭段落）] → 本文（見出し・リード画像・表と同じ1240px記事幅の1カラム。目次は列を取らず、デスクトップはヘッダー直下右上の固定ボタンからホバー・クリックで開き、現在の節を左罫と背景で示す）→ CTA → 関連3本 → LINE
```

## Signature（1つだけ）
**根拠バッジ**：記事カードと記事メタに「出典 N件」を等幅で表示する。本文の参考文献リンク数から自動算出。BARRELが「論文に基づく」ことを毎カードで証明する要素で、装飾ではなく情報。

## Motion
ページ表示時にリードブロックの eyebrow → 見出し → 要約が 60ms 刻みで立ち上がる、これ1回だけ。`cubic-bezier(0.16,1,0.3,1)`。スクロール連動フェードは全廃。`prefers-reduced-motion` 対応必須。

## ロゴ
`public/logo/barrel-logo.png`（緑＋ベージュ走者）を明るい面に、`barrel-logo-footer.png`（白抜き）を緑面に。改変しない。

## トークン規律
余白は `--s-1..--s-8`（4/8/12/16/24/32/48/64px）の8段のみ。角丸は 0 と 2px の2種（新聞の矩形感）。影は使わない（面の明度差と罫線で階層）。線幅 1px と 3px。
